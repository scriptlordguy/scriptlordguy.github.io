name: Build and publish Docker image

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          platforms: linux/amd64,linux/arm64

  # Optional: Render deploy template (commented). To enable, add RENDER_SERVICE_ID and RENDER_API_KEY as repo secrets and uncomment the job.
  # deploy-to-render:
  #   runs-on: ubuntu-latest
  #   needs: build-and-push
  #   steps:
  #     - name: Deploy to Render (template)
  #       run: |
  #         echo "Template: deploy to Render using API. Replace with your provider's invocation."
  #         # Example: curl -X POST -H "Authorization: Bearer $${{ secrets.RENDER_API_KEY }}" \
  #         #   -H "Content-Type: application/json" \
  #         #   -d '{"serviceId":"'$${{ secrets.RENDER_SERVICE_ID }}'","image":"ghcr.io/'${{ github.repository }}':latest"}' \
  #         #   https://api.render.com/deployments
